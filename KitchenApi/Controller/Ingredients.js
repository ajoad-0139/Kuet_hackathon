const uploadIngredients = async (req, res) => {};
const downloadIngredients = async (req, res) => {
  res.json({ msg: 'working.....' });
};

module.exports = {
  uploadIngredients,
  downloadIngredients,
};
